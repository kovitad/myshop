Round icon-only button; always pass `label` for accessibility.

```jsx
<IconButton label="Search" variant="secondary"><Icon name="search" /></IconButton>
```

Variants match Button (`primary`, `secondary`, `ghost`, `dark`). Default 44px.
