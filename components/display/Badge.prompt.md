Small uppercase pill. `silver` marks premium items; `green` is the default topic/status marker.

```jsx
<Badge tone="silver" icon={<Icon name="sparkles" size={11} />}>Premium</Badge>
<Badge>New guide</Badge>
```
