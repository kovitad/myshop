/** Silver hairline section divider (brand signature). */
export interface DividerProps {
  /** Optional centered uppercase label. */
  label?: string;
  /** Vertical margin in px. */
  spacing?: number;
  style?: React.CSSProperties;
}
