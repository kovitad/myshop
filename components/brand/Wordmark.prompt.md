Type-only brand lockup — use wherever a logo would go (no final logo exists yet).

```jsx
<Wordmark size="md" />            // header
<Wordmark size="sm" tone="dark" />  // dark footer
```

Never redraw the exploration marks in `assets/` as SVG; they are references only.
