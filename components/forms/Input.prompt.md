Labelled text input (44px, silver hairline, green focus ring).

```jsx
<Input label="Email" placeholder="you@example.com" icon={<Icon name="mail" size={16} />} help="No spam — one letter a week." />
<Input label="Email" error="Enter a valid email address." />
```
